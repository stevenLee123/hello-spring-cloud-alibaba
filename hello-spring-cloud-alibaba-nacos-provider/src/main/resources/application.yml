spring:
  application:
    name: service-provider
  cloud:
    nacos:
      discovery:
      #服务注册中心
        server-addr: peer1:8848
server:
  port: 8071
management:
  #健康检查
  endpoints:
    web:
      exposure:
        include: "*"
